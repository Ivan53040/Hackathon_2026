# 觀眾席 — 給 Codex

**問題：競技場有看台，但看台是空的。** 空看台讀起來是廢墟，不是「有在辦比賽的場地」。
海報已經掛上去了，場地在宣傳一場比賽 —— 但沒有半個人來看。

---

## 0. 🔴 兩條最重要的

### 0-1　**不准碰主角**

**`src/view/actors.ts` 完全不要開。**

那個檔管的是對手的 sprite、五個姿勢、32 格 action atlas、以及腳線對齊。
今天已經因為那個檔壞過三次：

- 第一次：只換了 idle 一張圖，畫布比例不同 → 其餘四個姿勢被**橫向拉寬 22%**，切姿勢時角色換人 + 變胖 + 上下跳
- 六張圖的腳線是**逐格算過**對齊到同一條基準的（世界座標誤差 0.0001）
- 蓄力停在第 15 格是因為 f16/f17 的水晶尖端在生成當下就被 tile 切平了

**動一下就會全部壞掉，而且不會報錯。** 角色素材與動畫由 Wesley 負責，你不要碰。

### 0-2　你只寫一個新檔

**`src/view/crowd.ts` —— 100% 是你的，已經建好也接好線了。**

```ts
export function buildCrowd(scene: THREE.Scene): void;   // 已由 view/index.ts 呼叫
export function updateCrowd(t: number, dt: number): void; // 已接進每幀迴圈
export function disposeCrowd(): void;                     // 已接進 disposeView
```

裡面附了 `tok()`、`track()`、`ownGeometry()`、`ownMaterial()`。

**不准開**：`actors.ts`（見上）、`romanArena.ts`、`index.ts`、`camera.ts`、`scenery.ts`、
`src/main.ts`、`src/match/`、`src/ui/`。

需要改既有的東西，**寫在回報最後，Wesley 來改。**

> 前兩次的 brief 也是這樣寫的，你兩次都越界了（`scenery.ts` 那次你改了 `actors.ts`、
> `view/index.ts`、`match/index.ts`）。這次請照做。

---

## 1. 場地座標（照抄）

```
相機（我）    x = 0,  y = 1.6,  z = 0        FOV 55°，視線水平
對手          z = −7.5                       🔴 這條視線不能被擋
對手的平台     z = −8.5
決鬥場地面     y = −0.65
橫向 lane     x ∈ [−3, 3]
場館海報      7.2 × 4.05，中心 (0, 5.8, −13)  ← 不要擋到它
```

**看台**在 `roman_arena_core.glb` 裡，是對手後方與兩側那幾層階梯。
你要自己 raycast 或目視抓出座標 —— 大致在 `|x| > 6` 的兩側，以及 `z < −10` 的後方，
高度隨階數往上。**先在畫面上放幾個測試方塊確認位置，再放人。**

**霧**：`Fog(--void, 14, 46)`。看台大多落在這個範圍內，會自然被霧吃掉一部分 —— 這是好事，
遠處的觀眾本來就該糊掉。

---

## 2. 美術方向：只有剪影，沒有人

一句話：**觀眾是一片會呼吸的暗色量體，不是一群角色。**

- **純剪影。** 沒有臉、沒有手、沒有裝備。一個頭 + 一個肩的輪廓就夠了
- **比石頭暗。** 他們是背景的量體，看台的石階要比他們亮，層次才出得來
- **高矮不一、間距不均。** 整齊排列會讀成柵欄不是人群
- **可以有空位。** 全滿反而假；留幾段空的，像真的場館

### 🔴 顏色的硬規則

**不准用 `--me`（金）或 `--them` / `--them-hot`（藍）。**

那是敵我識別色。看台上出現金色或藍色的人形，玩家會在餘光裡把它誤讀成對手 ——
這個遊戲的核心就是「看到對手起手就閃」，餘光被污染是會影響勝負的。

用 `--ash`、`--struct`、`--void` 之間的混色。整片壓在低飽和、低明度。

---

## 3. 優先序

| | 項目 | 時間 | 為什麼 |
|---|---|---|---|
| 🔴 **A** | 靜態剪影人群鋪滿看台 | 60 分 | 一項就解決「空場館」 |
| 🟡 **B** | 極慢的閒置擺動 | 20 分 | 完全靜止會讀成雕像。幅度要**很小** |
| 🟢 **C** | 命中時人群反應 | 30 分 | `on(EV.SPELL_HIT)` → 整片短暫亮一點點（≤8%，≤0.4s）。**只做到這樣**，不要做波浪 |

**時間不夠就只做 A。**

---

## 4. 硬規則（會被檢查）

- 🔴 **一定要用 `InstancedMesh`。** 幾百個獨立 `Mesh` 會直接把 fps 打死。
  一個 geometry + 一個 material + 一次 draw call
- 🔴 **不准加新的光源。** 現在有月光 + 半球 + 補光 + 4 盞火把點光。
  觀眾用 `MeshBasicMaterial` 或 `MeshLambertMaterial`，不要自己發光
- 🔴 **中間 1/3 畫面必須淨空。** 對手在 `z = −7.5`，玩家要隨時看得到他全身與頭頂數值
- **不准在 `updateCrowd` 裡配置物件**（每幀熱路徑）。要動的用 `instanceMatrix` / `instanceColor`
- **顏色一律從 `src/ui/tokens.css` 讀**，不准寫死 hex
- **不准引入新的 npm 套件**
- **不准加人群 GLB。** 建築已拆成 `roman_arena_core.glb` 與 `roman_arena_decor.glb`；人群仍然必須用程式生成的 `InstancedMesh`
- **不准改 `src/core/config.ts` 的數值**
- **不准 `git add` / `commit` / `push`**（見 `rules.md`）

---

## 5. 驗收

```bash
npm run build          # 必須零 error
```

```
https://localhost:5173/?solo=1
```

五項目視確認：

1. 🔴 **fps 還是 60**（左上角 HUD）。這是加人群最容易搞砸的地方。**掉到 55 以下就減量，不要猶豫**
2. 🔴 **對手全身清楚可見**，中間 1/3 沒有任何遮擋
3. 🔴 **餘光測試**：盯著對手，看台上不應該有任何東西讓你覺得「那邊也有一個人在動」
4. 海報沒有被擋到
5. 左右走位（A / D）時人群有視差，不會穿模

---

## 6. 做完回報

- 改了哪些檔（**應該只有 `src/view/crowd.ts` 一個**）
- fps 前 / 後、draw call 數
- 人群的總數量
- **需要 Wesley 從 `romanArena.ts` 改什麼** —— 列出來，不要自己動
- 有沒有沒解決的問題

**不要 commit，留給人類。**
