# view — 第一人稱渲染　[D]

畫面上**沒有你自己**，只有對手。所以對手承擔全部角色表演，相機承擔全部「我」的感受。
對手永遠在視線水平高度。FOV 55°。相機震動 ≤8px、≤0.25s，超過會暈。
`opponent.ts` 從 `public/anim/manifest.json` 讀 sprite atlas，**先用程式剪影上線**，素材後到再換。
投射物尺度曲線前 70% 慢、後 30% 暴衝 —— 這決定第一人稱好不好玩。
