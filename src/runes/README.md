# runes — 符文辨識　[B]

$1 Unistroke，自己寫，不裝套件。兩個符文：**△ 攻擊 / □ 建造**。
先用角點數分流（3 角 → △、4 角 → □），比調閾值有效十倍。
備案：若 △/□ 混淆率高，把 □ 改成一條水平線 `—`，template 現在就一起錄。
⚠️ `templates.json` **必須用 webcam pipeline 錄**，滑鼠錄的一律作廢 —— 抖動特性完全不同。
每符文 ≥8 樣本，至少 2 個不同的人錄。
